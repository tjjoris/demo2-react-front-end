import React, { Component } from "react";

// Main App component
class App extends Component {
    // Render method to display the component
    render() {
        return <div>
            App
        </div>
    }
}

export default App;